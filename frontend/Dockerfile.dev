# ===========================================
# Frontend Dockerfile (Development)
# ===========================================
# 핫리로드를 위한 개발용 Dockerfile
# Vite dev server with HMR

FROM node:22-alpine

WORKDIR /app

# 패키지 파일 복사 및 의존성 설치
COPY package*.json .

# 의존성 설치 (node_modules는 볼륨으로 관리)
RUN npm install

# 소스코드는 볼륨 마운트로 처리

EXPOSE 5173

# Vite 개발 서버 실행 (host 0.0.0.0으로 외부 접근 허용)
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
